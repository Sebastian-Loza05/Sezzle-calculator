server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location = /calculate {
        proxy_pass http://backend:8080;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
